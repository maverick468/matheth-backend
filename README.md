# Matheth Backend API

Node.js, Express, and Firebase backend supporting the Matheth gamified learning platform with dual-AI integration (Gemini + Groq) and Cloudinary asset management.

## Setup & Running

1. Copy `.env.example` to `.env` and fill in your configuration keys.
2. Install dependencies:
   ```bash
   npm install